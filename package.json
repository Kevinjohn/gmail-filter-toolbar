{
  "name": "gmail-calendar-options",
  "version": "1.0.0",
  "description": "Toggle visibility of calendar invites inside Gmail.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "lint": "eslint src --fix",
    "test": "jest"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "vite": "^5.0.0"
  }
}
